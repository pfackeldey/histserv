name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.14"]

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # Pre-build the dashboard UI so hatch_build.py finds dist/ during packaging.
      # uv/hatchling don't know about bun, so we must do this before the Python build.
      - uses: prefix-dev/setup-pixi@v0
        with:
          pixi-version: latest
          environments: dashboard
          cache: true

      - name: Bundle dashboard UI
        run: pixi run dashboard-bundle

      - uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --dev

      - name: Run pytest
        run: uv run pytest

  frontend:
    name: Frontend (${{ matrix.check }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        check: [test, lint, check, build]

    steps:
      - uses: actions/checkout@v6

      - uses: prefix-dev/setup-pixi@v0
        with:
          pixi-version: latest
          environments: dashboard
          cache: true

      - name: Run ${{ matrix.check }}
        run: pixi run dashboard-${{ matrix.check }}
